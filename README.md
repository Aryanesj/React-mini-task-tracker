hi. this is a mini project, the purpose of which is to use basic hooks in react
----------------------------------------------------------------------------------------------------------------------
npm install
npm run server     (http://localhost:5000/tasks) / db.json
npm start
